{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch WebSocket example",
            "program": "${workspaceFolder}/examples/ws.ts",
            "preLaunchTask": "tsc: build - tsconfig.json",
            "env": {
              "NODE_ENV": "development"
            },
            "skipFiles": [
                "<node_internals>/**"
            ],
            "outFiles": [
                "${workspaceFolder}/build/**/*.js"
            ]
        }
    ]
}
